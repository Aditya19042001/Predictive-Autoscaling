# This ConfigMap is generated by running:
#   kubectl create configmap grafana-dashboard-json \
#     --from-file=autoscaler-dashboard.json=k8s/grafana/dashboards/autoscaler-dashboard.json
# The script scripts/setup.sh handles this automatically.
#
# It is listed here as a placeholder so the Grafana deployment volume mount
# reference is always resolvable even if setup.sh has not been run yet.
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-json
  namespace: default
data: {}
